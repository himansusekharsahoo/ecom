name: CD

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Trigger Argo CD Sync
        run: |
          echo "Argo CD auto-sync will deploy changes"